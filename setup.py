from distutils.core import setup
setup(
  name = 'django-json',
  packages = ['django_json'], # this must be the same as the name above
  version = '0.5',
  description = 'Django-JSON conversion',
  author = 'Will Tennien-Murphy',
  author_email = 'wtmurp2@gmail.com',
  url = 'https://github.com/will-murphy/django-json', # use the URL to the github repo
  download_url = 'https://github.com/will-murphy/django-json/tarball/0.1', # I'll explain this in a second
  keywords = ['django', 'json'], # arbitrary keywords
  classifiers = [],
)
